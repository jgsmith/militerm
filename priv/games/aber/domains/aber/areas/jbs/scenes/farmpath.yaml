detail:
  default:
    exits:
      east:
        target: "scene:aber:jbs:front"
      west:
        guarded: true
        target: "scene:aber:jbs:gate"
    short: Path to the Farmhouse
    sight: >
      This path runs east to the farmhouse and west back into the pasture.
flag:
  - outdoors
